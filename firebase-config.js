/**
 * OPTIONAL: paste your Firebase web app config here to hard-wire the connection.
 *
 * Firebase Console → Project settings → General → Your apps → Web app → Config
 *
 * Example:
 * window.FIREBASE_CONFIG = {
 *   apiKey: "AIza....",
 *   authDomain: "your-project.firebaseapp.com",
 *   projectId: "your-project",
 *   storageBucket: "your-project.appspot.com",
 *   messagingSenderId: "1234567890",
 *   appId: "1:1234567890:web:abcdef"
 * };
 *
 * If you leave this as null, the app shows a one-time setup screen where you can
 * paste the config in the browser instead (it's remembered per device).
 */
window.FIREBASE_CONFIG = null;
